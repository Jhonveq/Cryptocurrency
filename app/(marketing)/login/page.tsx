"use client";

import LoginForm from "@/components/LoginForm";

const Login = () => {
  return (
    <div className="w-screen h-screen flex items-center justify-center font-mono bg-gradient-to-r from-slate-500 to-slate-800">
      <LoginForm />
    </div>
  );
};

export default Login;
